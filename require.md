------

## 1. 需求定义 (Requirement Specifications)

### 1.1 核心功能

- **高精度坐标映射**：无论当前窗口缩放比例如何，输出的坐标必须映射回原始图像的物理像素（例如：$4000 \times 3000$ 像素中的准确位置）。
- **无限缩放与平移**：支持以鼠标为中心的丝滑缩放，以及大图状态下的自由平移。
- **实时坐标预览**：鼠标移动时，实时显示当前指向的原始像素坐标。
- **坐标记录管理**：点击取点后，支持记录多个坐标，并能对已取点进行编辑或删除。

### 1.2 非功能性需求

- **低延迟渲染**：处理大图（10MB+）时，缩放和平移不得有肉眼可见的卡顿。
- **像素对齐可视化**：在极高缩放倍数下，需展示像素网格辅助线。

------

## 2. 交互设计 (Interaction Design)

### 2.1 核心操作逻辑（三位一体方案）

为了达到你要求的“丝滑”，我设计了**“准心 + 缩放 + 放大镜”**的联动方案：

| **动作**     | **触发方式**        | **视觉反馈**                    | **目的**                    |
| ------------ | ------------------- | ------------------------------- | --------------------------- |
| **精准定位** | 鼠标移动            | 全屏十字准心（Crosshair）       | 消除普通箭头对像素点的遮挡  |
| **视觉增强** | 鼠标悬停            | 侧边栏/跟随放大镜 (Magnifier)   | 放大鼠标周围 10x10 的像素块 |
| **视角变换** | 滚轮 / `Space`+左键 | 以鼠标为中心的平滑缩放/抓手平移 | 快速在全景与微观细节间切换  |
| **取点确认** | 鼠标左键单击        | 在原图对应位置打上“锚点”标记    | 完成坐标拾取                |

------

### 2.2 详细交互流程

#### A. 画布交互（Canvas Interaction）

1. **缩放（Zooming）**：
   - **交互**：向前滚动滚轮放大，向后缩小。
   - **细节**：**必须以鼠标当前物理位置为中心缩放**（锚点缩放），而不是以图片中心缩放。这是丝滑感的关键。
2. **平移（Panning）**：
   - **交互方式一（专业）**：长按鼠标右键并拖拽。
   - **交互方式二（通用）**：按下空格键（Space）时光标变为“抓手”，此时左键拖拽。
3. **坐标显示**：
   - 在状态栏实时显示：`Current (Original): [X, Y]`。

#### B. 像素级辅助——动态放大镜（The Magnifier）

- **逻辑**：在 UI 的右上角或跟随鼠标悬浮一个 200x200 像素的窗口。
- **内容**：显示原始图像在当前坐标周围的局部放大图（通常放大 5-10 倍），并在放大镜中心绘制一个固定的小红点。
- **效果**：用户不需要把主画布放大到 1000%，只需通过放大镜即可看清边界像素。

#### C. 点位管理（Point Management）

- **交互**：左键点击后，在图上生成一个带编号的半透明标记。
- **列表**：右侧提供一个 QListWidget，记录 `Index | X | Y | Color`。
- **联动**：点击列表中的坐标，画布自动平移并缩放至该点。

------

## 3. UI 界面布局建议

使用 PyQt5 的 `QMainWindow` 结构进行分栏：

- **顶部菜单栏**：文件（打开、保存坐标）、设置（辅助线颜色、放大镜倍率）。
- **中间核心区**：基于 `QGraphicsView` 的画布，背景为深灰色（Photoshop 风格），用于衬托图片。
- **右侧工具栏**：
  - **预览窗口**：显示全图缩略图及当前视口位置（红色框）。
  - **实时放大镜**：像素级的实时反馈。
  - **坐标列表**：保存已拾取的点位。
- **底部状态栏**：显示图片尺寸、当前缩放比例、实时原始坐标。

------

## 4. 技术实现要点（给开发者的 Tips）

1. **坐标转换公式**：

   不要手动计算 `event.pos() * scale`。在 PyQt5 中，将图片放入 `QGraphicsScene`，直接使用 `view.mapToScene(event.pos())`。它会自动处理所有缩放和平移的偏移量，返回的值直接就是原始图像的坐标。

2. **抗锯齿开关**：

   在缩放至极小时，开启 `SmoothPixmapTransform` 以防闪烁；但在缩放至极大看像素时，应关闭平滑缩放，让像素点保持“方块状”，方便精确定位。

3. **大图性能**：

   如果图片超过 50MB，建议使用 `QImage` 的 `scaled()` 预生成多级索引，或者直接利用 `QGraphicsView` 的二进制空间分割（BSP）管理。